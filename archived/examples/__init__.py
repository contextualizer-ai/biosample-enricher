"""Example scripts for biosample-enricher.

These demonstration scripts show how to use various features of the
biosample-enricher package. They are not installed with the package
by default (they're excluded from the wheel/sdist) but can be run
from a development installation.

To run a demo script:
    uv run nmdc-adapter-demo
    uv run weather-demo
    etc.

Or run directly:
    python examples/weather_demo.py
"""
